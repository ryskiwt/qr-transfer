# QR Transfer

QRコード起点のPC・スマートフォン間ファイル転送アプリです。

## 使い方

1. PCでGitHub PagesのURLを開きます。
2. 表示されたQRコードをスマートフォンで読み取ります。
3. スマートフォン側でファイルを選択するか、カメラで撮影して送信します。
4. PC側で受信したファイルをプレビューし、必要に応じて保存します。

PC側のURLには固定の`room`パラメータが付きます。同じPCブラウザでは接続IDを維持するため、PC側をリロードしてもスマートフォン側は同じURLのまま再接続できます。
